/**
 * LiveSessionManager has been removed to eliminate real-time voice conversation features.
 * This file is now empty to prevent import errors if any stale references existed.
 */
export const LiveSessionManager = () => null;
export default LiveSessionManager;